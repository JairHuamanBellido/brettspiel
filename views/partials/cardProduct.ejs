<div class="card-product-form">

    <%  if(isUserAuthenticated){ %>
    <form action="/createBill/<%=product.id%>/<%=user.id%>" method="POST">

        <%} else{ %>
        <form action=""></form>
        <% }%>
        <div id="first-display-card">
            <h1 id="price-product"><%= product.price%></h1>
            <p>Costo de alquiler por dia</p>
            <div class="fiel">
                <div class="field-recogida">
                    <label for="">Fecha de recogida</label><br>
                    <input id="startRentDate" type="date" name="fechaDeRecogida" placeholder="Ingrese la fecha de recogida">
                </div>
                <div class="field">
                    <label for="">Fecha de Entrega</label><br>
                    <input id="endRentDate" onchange="setEndRentDate(this)" type="date" name="fechaDeEntrega" placeholder="Ingrese la fecha de entrega">
                </div>
            </div>
            <div class="snacks-container-card">
                <a onclick="openModalSnacks()">Desea agregar Snacks</a>
            </div>
            <div class="field">
                <label for="">Cantidad de productos</label>
                <input type="text" onkeyup="setQuantityProduct(this)" name="cantidadDeProductos" value="1" type="number" id="CantProduct">
            </div>

            <% snacks.forEach( snack=>{ %>
                <input type="text" style="display: none;" value="false" name="<%=snack.name %>" id="<%= snack.name%>field">
            <% })  %>
            <div class="bill-card">
                <h1>Boleta</h1>
                <div class="result-Snacks">
                    <p>Snacks</p>
                    <p>S/ <span id="TotalSnacks">0.00</span></p>
                    <input id="fieldTotalSnack" style="display: none;" name="SnacksTotal" value="0.00" type="text">
                </div>
                <div class="result-rent">
                    <p>Total de alquiler</p>
                    <p>S/ <span id="TotalRent"><%= product.price %></span></p>
                    <input style="display: none;" name="RentTotal" id="FieldTotalRent" type="text" >
                </div>
                <div class="result-total">
                    <p>Total</p>
                    <p>S/ <span id="FieldTotalOrder"><%=product.price%></span></p>
                </div>
            </div>
            <%  if(isUserAuthenticated){ %>
            <a onclick="ContinueWithPayment()">Continuar</a>
            <% } else{ %>
                <a onclick="buy()">Continuar</a>
            <% } %>
        </div>
        <div id="second-display-card">
            <h1>Pago con tarjeta</h1>
            <p>Completa los siguientes campos para adquirir el juego de mesa</p>

            <div class="field">
                <label for="">Numero de tarjeta</label>
                <input name="numeroDeTarjeta" type="text" placeholder="1234 3456 1231 3434">
            </div>
            <div class="field">
                <div class="sub-field">
                    <label for="">Fecha de expiracion</label>
                    <input name="fechaDeExpiracionTarjeta" type="text" placeholder="MM/AA">

                </div>
                <div class="sub-field">
                    <label for="">CCV</label>
                    <input name="CCV" type="text" placeholder="CCV">
                </div>
            </div>

            <div class="boleta-detail">
                <h1>Boleta</h1>
                <div class="field-camp">
                    <p>Snacks: </p>
                    <p>S/ 0.00</p>
                </div>
                <div class="field-camp">
                    <p>Alquiler por día: </p>
                    <p>S/ 0.00</p>
                </div>
                <div class="field-camp-result">
                    <p>Total: </p>
                    <p>49.95</p>
                </div>
            </div>

            <div class="field-payBill">
                <button>Pagar</button>
            </div>

        </div>
    </form>

    <div id="waitingLoadCard">

        <div id="loader-container">
            <div id="loader">
            </div>
        </div>


        <p>Cargando...</p>

    </div>


</div>
